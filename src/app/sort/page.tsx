import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Sort',
  description: 'Popular sort algorithms',
};

export default function Sort() {
  return <div>Hello from the sort</div>;
}
